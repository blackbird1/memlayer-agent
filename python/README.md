# Python Implementation

This folder is reserved for the Python implementation of the MemLayer agent.

Structure:

- `src/memlayer_agent/` for package code
- `pyproject.toml` for Python build and tooling metadata
- `AGENTS.md` for Python-specific working conventions

The Python implementation is intentionally separate from the Go implementation in `../go/`.
