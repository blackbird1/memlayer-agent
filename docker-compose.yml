services:
  redis:
    image: redis:7-alpine
    container_name: adk-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  adk-server:
    build:
      context: .
      dockerfile: Dockerfile.go
    container_name: adk-server
    restart: unless-stopped
    profiles: [go]
    env_file:
      - .env
    environment:
      REDIS_ADDR: redis:6379
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "9090:9090"
    depends_on:
      redis:
        condition: service_healthy

  adk-server-python:
    build:
      context: .
      dockerfile: Dockerfile.python
    container_name: adk-server-python
    restart: unless-stopped
    profiles: [python]
    env_file:
      - .env
    environment:
      REDIS_ADDR: redis:6379
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "9091:9090"
    depends_on:
      redis:
        condition: service_healthy

  adk-web:
    build:
      context: .
      dockerfile: Dockerfile.web
    container_name: adk-web
    restart: unless-stopped
    environment:
      API_BACKEND: ${API_BACKEND:-adk-server:9090}
    ports:
      - "8080:8080"
    depends_on:
      redis:
        condition: service_healthy
